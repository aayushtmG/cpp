                                     //conversion between celsius and fahrenheit
#include <iostream>
using std::cout, std::cin,std::endl;

/*
int ctof(int input){
    int result = (input * (9.0/5)) + 32;
    cout << result << " from function "<< endl;
    return result;
}

float ftoc(int input){
    float result = (input - 32) * (5.0/9);
    return result;
}

int main(){
    int input;
    char option;
    int result;
    cout << "Choose C if you want to convert Celsius to Fahrenheit or F to convert Fahrenheit to celsius" << endl;
    cin >> option;
    if(option == 'C' || option =='c'){
        cout << "Enter the number: ";
        cin >> input;
        result = ctof(input); 
        cout << input << " in Fahrenheit " << "is: " << result << endl; 
    }else if(option == 'F' || option == 'f'){
        cout << "Enter the number: ";
        cin >> input;
        float result = ftoc(input);
        cout << input << " in Celsius " << "is: " << result << endl; 
    }else{
        cout << "Invalid option!!!" << endl; 
    }
    return 0;
}*/




                                     int main(){
                                         int a = 13;
                                         int b = 5;
                                         float c = (float)a/b;
                                         cout << c << endl;
                                         return 0;
                                     }




